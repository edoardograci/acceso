---
import { normalizeImage } from '../../lib/images';

interface Props {
  university: {
    name: string;
    image?: string;
    city_slug?: string;
    country_slug?: string;
  };
  cityName: string;
  countryName: string;
}

const { university, cityName, countryName } = Astro.props;
---

<header class="school-header">
  <div class="school-header-identity">
    <div class="school-logo-box">
      {university.image ? (
        <img
          src={normalizeImage(university.image, Astro, { width: 240 })}
          alt={university.name}
          class="school-logo-img"
          loading="eager"
          fetchpriority="high"
          decoding="async"
        />
      ) : (
        <div class="school-logo-fallback">
          <span>{university.name.charAt(0)}</span>
        </div>
      )}
    </div>

    <div class="school-header-text">
      <h1 class="school-title">{university.name}</h1>
      <p class="school-subtitle">
        {university.city_slug ? (
          <a href={`/directory/schools/in/${university.city_slug}`} class="subtitle-link">{cityName}</a>
        ) : (
          <span>{cityName}</span>
        )}
        {countryName && cityName !== countryName && (
          <>
            <span class="subtitle-sep">,</span>
            {university.country_slug ? (
              <a href={`/directory/schools/in/${university.country_slug}`} class="subtitle-link">{countryName}</a>
            ) : (
              <span>{countryName}</span>
            )}
          </>
        )}
      </p>
    </div>
  </div>

  <div class="school-header-actions">
    <button class="header-share-btn" id="shareButton" type="button" aria-label="Share this school">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8"/>
        <polyline points="16 6 12 2 8 6"/>
        <line x1="12" y1="2" x2="12" y2="15"/>
      </svg>
      <span>Share</span>
    </button>
  </div>
</header>

<script>
  const shareButton = document.getElementById('shareButton');
  if (shareButton) {
    shareButton.addEventListener('click', async () => {
      const shareData = {
        title: document.title,
        url: window.location.href,
        text: `Check out ${document.title.split('—')[0]} on Acceso`
      };
      try {
        if (navigator.canShare && navigator.canShare(shareData)) {
          await navigator.share(shareData);
        } else {
          await navigator.clipboard.writeText(window.location.href);
          alert('Link copied to clipboard');
        }
      } catch (err) { }
    });
  }
</script>

<style>
  .school-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    padding: 16px 0 24px;
    border-bottom: none;
    margin-bottom: 24px;
    width: 100%;
    box-sizing: border-box;
  }

  .school-header-identity {
    display: flex;
    align-items: center;
    gap: 20px;
    min-width: 0;
    flex: 1;
  }

  .school-logo-box {
    flex-shrink: 0;
    width: 76px;
    height: 76px;
  }

  .school-logo-img {
    width: 76px;
    height: 76px;
    border-radius: 14px;
    border: 1px solid var(--color-stroke);
    object-fit: contain;
    background: var(--color-white);
    display: block;
    padding: 0; /* No padding inside logo box */
  }

  .school-logo-fallback {
    width: 76px;
    height: 76px;
    border-radius: 14px;
    border: 1px solid var(--color-stroke);
    background: var(--color-bg-secondary);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .school-logo-fallback span {
    font-family: var(--font-sans);
    font-size: 32px;
    font-weight: 300;
    color: var(--color-gray);
    text-transform: uppercase;
  }

  .school-header-text {
    min-width: 0;
    flex: 1;
  }

  .school-title {
    font-family: var(--font-sans);
    font-size: clamp(24px, 3.2vw, 36px);
    font-weight: 400;
    letter-spacing: -0.02em;
    line-height: 1.15;
    color: var(--color-black);
    margin: 0 0 6px;
    word-break: break-word;
  }

  .school-subtitle {
    font-family: var(--font-sans);
    font-size: 15px;
    color: var(--color-gray);
    margin: 0;
    display: flex;
    align-items: center;
    gap: 4px;
    flex-wrap: wrap;
  }

  .subtitle-link {
    color: var(--color-gray);
    text-decoration: underline;
    text-underline-offset: 2px;
    transition: color 0.2s ease;
  }

  .subtitle-link:hover {
    color: var(--color-black);
  }

  .subtitle-sep {
    color: var(--color-gray);
    margin-right: 2px;
  }

  .school-header-actions {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-shrink: 0;
  }

  .header-share-btn {
    height: 40px;
    padding: 0 18px;
    border-radius: 999px;
    border: 1px solid var(--color-stroke);
    background: var(--color-bg-secondary);
    color: var(--color-black);
    font-family: var(--font-sans);
    font-size: 15px;
    font-weight: 700;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    transition: background-color 0.2s ease, border-color 0.2s ease;
  }

  .header-share-btn:hover {
    background: #EBEBED;
    border-color: #D4D4D8;
  }

  @media (max-width: 768px) {
    .school-header {
      gap: 16px;
      padding-bottom: 16px;
      margin-bottom: 20px;
    }
    .header-share-btn {
      width: 40px;
      height: 40px;
      padding: 0;
      border-radius: 50%;
    }
    .header-share-btn span {
      display: none;
    }
    .header-share-btn svg {
      width: 18px;
      height: 18px;
    }
  }

  @media (max-width: 560px) {
    .school-logo-box {
      width: 60px;
      height: 60px;
    }
    .school-logo-img, .school-logo-fallback {
      width: 60px;
      height: 60px;
      border-radius: 12px;
    }
    .school-logo-fallback span {
      font-size: 22px;
    }
    .school-title {
      font-size: 20px;
    }
  }
</style>
